# get_slide_file_name() works

    Code
      get_slide_file_name(pptx, index = 0)
    Condition
      Error in `get_slide_file_name()`:
      ! `index` is out of bounds: 0.
      i Presentation has 2 slides.

---

    Code
      get_slide_file_name(pptx, index = 3)
    Condition
      Error in `get_slide_file_name()`:
      ! `index` is out of bounds: 3.
      i Presentation has 2 slides.

