# check_pkg_version()

    Code
      check_pkg_version("mirage", larger_ref_version)
    Message
      This script was generated assuming version Y.Y.Y of "mirage".
      i You have version X.X.X installed.
      i Things might still work, but if it doesn't this might be worth exploring.

